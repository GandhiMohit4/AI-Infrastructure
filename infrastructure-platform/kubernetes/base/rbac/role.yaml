apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: application-read-only
  namespace: application
rules:
  - apiGroups: [""]
    resources:
      - pods
      - services
      - configmaps
    verbs:
      - get
      - list
      - watch