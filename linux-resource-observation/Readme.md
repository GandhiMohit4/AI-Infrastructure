# Linux Resource Observatory

A lightweight Linux resource monitoring system built in Python that collects
system-level resource metrics from the Linux `/proc` filesystem, exposes them
as Prometheus metrics, and visualizes them using Grafana.

The project is designed as a hands-on infrastructure observability project
covering Linux internals, Python, Prometheus, Grafana, metrics collection,
and monitoring.
